import { StyleSheet} from 'react-native';
import { APP_COLORS } from '../../styles/color';
export const style = StyleSheet.create({
  itemlist1: {
    backgroundColor: '#FF8Cf0',
    color: '#fff',
  },
  itemlist2:{
    backgroundColor: '#FF8C00',
    color: '#fff',
  }
});
